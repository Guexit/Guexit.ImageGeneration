version: '3.8'

services:
  app:
    build: .
    image: your-image-name:latest
    ports:
      - "8000:8000"
    env_file: .env
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
